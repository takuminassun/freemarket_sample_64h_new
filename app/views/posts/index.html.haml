.wrapper
  = render "header"
  = render "slick"
  = render "contents"
  = render "footer"
  -#= render "signup-header"

  -# = render "footer"
  -# = render "signup-header"
  -# = render "profile"
  -#= render "product-listing"

