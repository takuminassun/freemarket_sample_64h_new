.wrapper
  = render "header"
  = render "slick"
  -# = render "contents"
  -# = render "mypage"
  -# = render "signup-header"
  -# = render "profile"
  -# = render "product-listing"
  -# = render "purchase"
  = render "user-confirmation"
  = render "footer"
